/* reverse string
*/

var ab = "";
/*var character = ab.charAt(0);
console.log(character);

var lastCharacter = ab.charAt(ab.length - 2);
console.log(lastCharacter);*/

function reversing (ab) {
	for (var i = ab.length -1; i >= 0; i--) {
		var reverseCharacters = ab.charAt(i, -i);
		console.log(reverseCharacters);
	}
}
reversing("helio")
reversing('a');
reversing('');
